<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="PrismCourseApp.Views.MainPage"
             Title="MainPage">
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <Entry Text="{Binding CityName}"></Entry>
        <Button Text="Go to API" Command="{Binding GoToAPI}"></Button>
        <Switch IsToggled="{Binding IsActive}"></Switch>
        <Button Text="Go to second page" Command="{Binding GoToSecond}"></Button>
        <Button Text="Show MSg" Command="{Binding CallMsg}"></Button>
        <Button Text="Show Action Sheet" Command="{Binding DisplayActionSheet}"></Button>
        <Button Text="Go To Depedency Page" Command="{Binding GoDs}"></Button>
    </StackLayout>
</ContentPage>